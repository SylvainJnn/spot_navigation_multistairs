<?xml version="1.0"?>
<launch>
	<arg name="map_file" default="$(find multimap_server)/config/my_env.yaml"/>

	<!-- Run the map server -->
	<node name="multimap_server" pkg="multimap_server" type="multimap_server" args="$(arg map_file)">
	</node>

</launch>